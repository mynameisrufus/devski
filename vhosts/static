#!/usr/bin/env bash

SERVER_NAME=$1
APP_PATH=$2

DEVSKI_ROOT="${HOME}/.devski"

read -r -d '' SERVER_CONF <<EOF
# vhost generated by devski

server {
  listen 80;
  listen 443 ssl;
  ssl_certificate     $DEVSKI_ROOT/nginx/certs/dev.crt;
  ssl_certificate_key $DEVSKI_ROOT/nginx/certs/dev.key;
  server_name *.$SERVER_NAME.dev $SERVER_NAME.dev;
  root   $APP_PATH;
  autoindex on;
  index  index.html index.htm;

  error_page   500 502 503 504  /50x.html;
  location = /50x.html {
      root   html;
  }
}
EOF

echo "$SERVER_CONF"
